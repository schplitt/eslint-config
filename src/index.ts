import type { Config } from "./types";

/**
 * Create an ESLint flat config with sensible defaults.
 * @returns Array of ESLint flat config objects
 */
export function schplitt(): Config[] {
    return [
        {
            
        }
    ];
}

export default schplitt;